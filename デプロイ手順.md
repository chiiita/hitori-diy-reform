# リフォーム手順書サイト — 公開手順（GitHub Pages）

このフォルダ全体が独立したサイトです（内蔵ディスク・外付け不要）。
公開先: https://chiiita.github.io/hitori-diy-reform/  ※公開（URLを知る人は誰でも閲覧可）

## 初回：公開する
ターミナルでこのフォルダに移動して、上から順に実行:

    cd ~/hitori-diy-reform
    git init
    git add -A
    git commit -m "リフォーム手順書サイト（HitoriDiy 127本）"
    gh repo create hitori-diy-reform --public --source=. --push

※ commit で「Please tell me who you are」と出たら、先に一度だけ:
    git config --global user.name "chiiita"
    git config --global user.email "あなたのメール"
    その後 git commit をやり直す。

## Pages を有効化（初回のみ）
    gh api -X POST repos/chiiita/hitori-diy-reform/pages -f "source[branch]=main" -f "source[path]=/"
（またはブラウザで github.com → リポジトリ → Settings → Pages → Source=main / (root)）

1〜3分で https://chiiita.github.io/hitori-diy-reform/ が見られるようになります（スマホでもOK）。

## 更新（いつでも改善）
サイトを直したら:

    cd ~/hitori-diy-reform
    git add -A
    git commit -m "更新"
    git push

数十秒〜数分で反映されます。

## メモ
- 「改善して」と頼むときは、このフォルダ（~/hitori-diy-reform）が最新の正本です。
- start-local.command をダブルクリックすれば、公開せず手元(同じWi-Fi)でも確認できます。
- 公開なので、@HitoriDiy さんの動画フレームがネット上に出る点は理解の上で運用してください。
